import { OperatingSystemsPipe } from './operating-systems.pipe';

describe('OperatingSystemsPipe', () => {
  it('create an instance', () => {
    const pipe = new OperatingSystemsPipe();
    expect(pipe).toBeTruthy();
  });
});
