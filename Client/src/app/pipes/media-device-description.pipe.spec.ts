import { MediaDeviceDescriptionPipe } from './media-device-description.pipe';

describe('MediaDeviceDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new MediaDeviceDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
